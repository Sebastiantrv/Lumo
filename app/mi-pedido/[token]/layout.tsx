import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tu pedido · LUMO",
};

export default function MiPedidoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
